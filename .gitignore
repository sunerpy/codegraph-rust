.omo
.opencode
/target/
# Root /target/ is anchored and does not cover the isolated Zed extension crate.
editors/zed/target/
**/*.rs.bk

# Benchmark corpora are fetched on demand (codegraph-bench), not committed.
bench/corpora/

# Locally-built release binary (see README install/build instructions).
/codegraph
dist/

# Coverage artifacts (cargo-llvm-cov): lcov file + HTML report.
lcov.info
target/llvm-cov/

# Per-project index DB written by `codegraph init` / `index`.
.codegraph/

# Local upstream-sync ledger (colby-upstream-sync skill); not part of the repo.
docs/upstream-sync/

# AWS CodeBuild local config (sensitive — never commit)
.aws/codebuild.env
.aws/*.local
.aws/*.rendered.local.json
.aws/pipeline.rendered*
